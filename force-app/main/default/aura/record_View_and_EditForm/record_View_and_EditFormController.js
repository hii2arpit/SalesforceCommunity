({
    handleSuccess : function(component, event, helper) {
        var fields = component.get("v.fields");
        console.log('fields value-->'+fields);
        //alert('After saving the record');
    }
})